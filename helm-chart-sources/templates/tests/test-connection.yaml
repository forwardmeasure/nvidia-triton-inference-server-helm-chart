apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triton-inference-server.fullname" . }}-test-connection"
  labels:
    {{- include "triton-inference-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "triton-inference-server.fullname" . }}:{{ .Values.inference_deployment.port }}']
  restartPolicy: Never
